# Проверка требований — 23.09.2026

## Обновление интеграции — 17:44 Алматы

Объединены backend и frontend. Пройдены 96 backend-тестов, 10 браузерных тестов frontend с подставленным API и 4 браузерных интеграционных сценария с настоящим синтетическим API; обе сборки проходят. Интеграционные сценарии запускаются из `apps/web`: `npx playwright test --config playwright.integration.config.ts` (после `npm ci` в обоих приложениях; API 3010 и frontend 5180 запускаются автоматически, внешние AI-вызовы отключены).

Закрыты два замечания предыдущего среза: у каждой позиции предложения есть собственное количество, а изменение количества сохраняет остальные позиции и требует нового подтверждения; PNG принимается frontend и проходит тот же отдельный запрос согласия, что JPEG. Настоящим API также проверены пять основных сценариев, мобильный RU/KK, предел остатка и PDF → ручная проверка → отдельное подтверждение → актуальная корзина. До подтверждения корзина не менялась.

Ограничения по реальной корзине ekt.kz, неподтверждённым полям сертификата/цены и полноте live-каталога остаются. Чистый общий клон коммита `158f8c9` проверен в `/tmp/hackalem-final-integration-20260923`: `npm ci` из каталогов обоих приложений, обе сборки, 96 backend-тестов, 10 frontend-тестов и 4 браузерных сценария с настоящим синтетическим API прошли. Команды запуска и проверок согласованы с корневым README. Этот финальный прогон не использовал внешний OpenAI и не подтверждает его доступность.

## Исторический срез — 17:29 Алматы

Ниже сохранены результаты и открытые вопросы на момент прежней проверки. Замечания о нескольких количествах и PNG закрыты обновлением выше.


Срез: `origin/backend` = `21e6b489`, `origin/frontend` = `c3931969`. Проверка выполнена в отдельном worktree: frontend + `git merge --no-commit origin/backend` без конфликтов. Это проверка прототипа на синтетических данных, а не подтверждение интеграции корзины ekt.kz.

## Выполненные проверки

- Чистый `npm ci` в `apps/api` и `apps/web`; обе сборки прошли, включая отдельный JS виджета. Backend: `npm test` — 95/95. Frontend: `npm run test:e2e` — 10/10 на свежей ветке (API в этих браузерных тестах подставлен). `npm audit --omit=dev` для web — 0 уязвимостей.
- Независимый API-прогон проверил товар, аналог, публичные условия, многострочное предложение и повтор с тем же ключом идемпотентности. В браузере объединённой сборки `c3931969` проверены предложение без изменения корзины, отдельное подтверждение двух единиц `ABC-123` и актуальная страница `/cart`.
- В браузере на ширине 360 px виджет открывается на макете сайта без горизонтального переполнения; проверены предложение/подтверждение, переход в корзину и новый казахский ответ backend после переключения языка.
- JPEG без согласия дал ручную проверку. После отдельного согласия синтетическое фото с `DEMO-CABLE-10` прошло через настоящий OpenAI vision и свежую карточку демо-каталога; корзина после загрузки осталась пустой. Реальные клиентские фото и данные партнёра в этом прогоне не отправлялись.
- Доступность `gpt-6-luna`, `gpt-6-sol`, `gpt-6-astra` по локальному ключу подтверждена запросами к API; по одному ограниченному синтетическому текстовому вызову каждого уровня и vision-вызов Sol завершились успешно. В приложении известный артикул, условия покупки и корзина обходятся без модели.

## Матрица приёмки

| Требование | Проверенный результат | Предел проверки |
| --- | --- | --- |
| T1 товар, остаток, характеристики, сертификат | Демо-факты и отсутствие сертификата показаны честно; backend тестирует имеющуюся ссылку | Поле сертификата в live API ещё не подтверждено |
| T2 совместимый аналог с причиной | `ABC-000` → `ABC-124` с причиной и проверкой наличия | Live-аналог может отсутствовать без категории и критичных свойств |
| T3 условия покупки | Ответ с публичным источником и датой; неизвестные минимальная партия и спорный порог не выдумываются | Нужны подтверждения партнёра для неизвестных условий |
| T4–T5 согласие и актуальная корзина | В объединённом браузере корзина 0 до кнопки и 2 после; `/cart` показывает те же 2 | Для нескольких позиций UI пока не показывает количество каждой перед кнопкой |
| T6–T8 остаток, повтор, отказ и fallback | Backend-тесты проверяют повторную проверку остатка, атомарность, идемпотентность, сессии и ошибки | После общего финального исправления повторить браузерный прогон |
| T9 мобильность и вложения | 360 px без переполнения; JPEG с согласием/без него проверен через UI+API; PDF/DOCX/XLSX проверены backend-тестами и frontend E2E с подставленным API | PNG поддержан API, но отклоняется текущим выбором файла frontend; реальные сканы/фото разных товаров и все документные форматы через объединённый браузер не прогонялись |

Дополнения пользователя: RU/KK, макет сайта и встраиваемый виджет, маршрутизация по уровням моделей, контекст последних товаров, запросы нескольких категорий и целого дома реализованы в коде и покрыты соответствующими тестами. Запрос «целый дом» задаёт уточняющие вопросы и показывает ограниченный набор проверенных кандидатов; это не готовый электротехнический проект.

## Что остаётся до заявления о полной готовности

1. В `apps/web/src/assistant/Assistant.tsx` панель для `proposal.items` показывает названия всех позиций, но количество и редактор только для первой. В объединённом браузере воспроизведено на `2 DEMO-CABLE-10 + 1 DEMO-SOCKET-16`. Нужно показывать количество каждого товара и добавить браузерный тест. Тот же файл отклоняет PNG в проверке имени, согласии на фото и `accept`, хотя контракт API разрешает PNG. На [задаче frontend #5](https://github.com/BAITC-Hacks/hack-0571bab2-any/issues/5#issuecomment-5794742296) оставлена актуальная проверка.
2. Локальный индекс содержит 15 037 уникальных строк, но страница 77 партнёрского API повторяет первую. Полнота каталога не доказана. Не подтверждены поле сертификата, денежные единицы цены и API реальной корзины ekt.kz. До получения схемы и доступа приложение обязано сохранять пометку демо-корзины и неизвестных фактов.
3. После исправления пункта 1 повторить T1–T9 вместе с frontend-участником из чистого клона, проверить README и выполнить финальный браузерный сценарий на обеих ширинах. Текущий изолированный merge доказывает совместимость на этом срезе, но не является финальной сдачей.

## Проверка OpenAI с локальным ключом — 23.09.2026, 17:50 Алматы

Два запроса через серверный `/api/chat` с настоящим OpenAI gateway и синтетическим каталогом: русский «Подбери кабель» и казахский «Кабель керек». Оба вернули HTTP 200, успешный `light`-ответ модели и подтверждённые карточки каталога; корзина осталась пустой. Всего ровно 2 внешних вызова, 483 входных и 106 выходных токенов; длительность 4,16 и 2,98 с. Ключ прочитан только из локального игнорируемого `apps/api/.env`, его значение не выводилось. Реальный каталог партнёра и NVIDIA этим прогоном не проверялись.
